# ======================================
# SETUP INSTRUCTIONS - DIGISIGN
# ======================================

# Database - XAMPP MySQL Lokal
# Buat database 'digisign_db' di phpMyAdmin sebelum menjalankan push
DATABASE_URL="mysql://root:@localhost:3306/digisign_db"

# JWT Secret (Gunakan string acak yang panjang, min 32 karakter)
JWT_SECRET="masukkan-secret-key-anda-minimal-32-karakter"

# App URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# (Opsional) Vercel Blob - Hanya jika ingin deploy ke Vercel dengan Blob storage
# BLOB_READ_WRITE_TOKEN="your-vercel-blob-token"

# ======================================
# CARA MENJALANKAN LOKAL:
# ======================================
# 1. Start Apache & MySQL di XAMPP
# 2. Buat database 'digisign_db' di phpMyAdmin
# 3. Jalankan: npm install
# 4. Jalankan: npx prisma db push
# 5. Jalankan: npx prisma db seed
# 6. Jalankan: npm run dev
